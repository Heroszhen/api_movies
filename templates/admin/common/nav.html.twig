<nav class="navbar navbar-expand-lg fixed-top ps-3 pe-3" id="admin-nav">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Movies</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        {% set route = app.request.get('_route') %}
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link  {% if route == 'admin_home' %}active{% endif %}" href="{{ path('admin_home') }}" aria-current="page" href="#">Home</a>
                </li>
                <li class="nav-item dropdown">
                    {% set api_routes = [
                        'admin_actresses', 
                        'admin_videos',
                        'admin_movies',
                        'admin_users'
                    ] %}
                    <a class="nav-link dropdown-toggle {% if route in api_routes %}active{% endif %}" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        APIs
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item {% if route == 'admin_actresses' %}active{% endif %}" href="{{ path('admin_actresses') }}">Actrices</a></li>
                        <li><a class="dropdown-item" href="#">Videos</a></li>
                        <li><a class="dropdown-item {% if route == 'admin_movies' %}active{% endif %}" href="{{ path('admin_movies') }}">Films</a></li>
                        <li><a class="dropdown-item {% if route == 'admin_photos' %}active{% endif %}" href="{{ path('admin_photos') }}">Photos</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item {% if route == 'admin_users' %}active{% endif %}" href="{{ path('admin_users') }}">Comptes</a></li>
                    </ul>
                </li>
            </ul>
            <div class="d-flex right-part">
                <div class="dropdown">
                    <div class="dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                       {{ app.user.lastname }} {{ app.user.firstname }}
                    </div>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Mon profile</a></li>
                        <li><a class="dropdown-item" href="/logout">Déconnexion</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
